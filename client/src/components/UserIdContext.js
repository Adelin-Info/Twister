import { createContext } from 'react';

// Création d'un contexte permettant stockant l'id de la personne connecter pour y accéder tout au long de sa navigation
export const UserIdContext = createContext();